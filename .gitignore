node_modules/
dist/
build/
coverage/
.next/
out/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

.DS_Store
*.log
npm-debug.log*

# Ajustes especificos da maquina do operador (portas em uso, CA de proxy TLS
# local). O compose base deve continuar funcionando sozinho.
docker-compose.override.yml
docker/certs/

# Sessao do affiliate-bot: cookies reais de uma conta. Nunca versionar.
.garimpo/
affiliate-session.json
**/affiliate-profile/
*.playwright-profile/
# `/profile` e o caminho padrao dentro do container; se alguem montar aqui,
# tambem fica fora do git.
/profile/
